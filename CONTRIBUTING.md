# Contributing

Keep benchmark changes reproducible and add regression tests for every behavioral change. Do not expose reference implementations or hidden defect labels to agent contexts. New benchmark claims must distinguish visible-test plausibility from held-out correctness.
